# AD9238 模块外部引脚连接说明

本文对应工程�?
```text
D:\wenjian\dpj\AD9238_DDS_data_read
```

核心采集链路�?
```text
AD9238 D0~D11 -> STM32H7R7 PSSI_D0~D11
TIM1_CH1      -> AD9238 CLK
AD9238 DCO    -> PSSI_PDCK
PSSI + GPDMA  -> raw_buf[]
```

## 1. 必须连接的信�?
| AD9238 模块信号 | 连接�?STM32H7R7 引脚 | STM32 功能 | 说明 |
| --- | --- | --- | --- |
| `D0` | `PA9` | `PSSI_D0` | 并行数据 bit0 |
| `D1` | `PA10` | `PSSI_D1` | 并行数据 bit1 |
| `D2` | `PE0` | `PSSI_D2` | 并行数据 bit2 |
| `D3` | `PE1` | `PSSI_D3` | 并行数据 bit3 |
| `D4` | `PC11` | `PSSI_D4` | 并行数据 bit4 |
| `D5` | `PD3` | `PSSI_D5` | 并行数据 bit5 |
| `D6` | `PB8` | `PSSI_D6` | 并行数据 bit6 |
| `D7` | `PB9` | `PSSI_D7` | 并行数据 bit7 |
| `D8` | `PF4` | `PSSI_D8` | 并行数据 bit8 |
| `D9` | `PF3` | `PSSI_D9` | 并行数据 bit9 |
| `D10` | `PD6` | `PSSI_D10` | 并行数据 bit10 |
| `D11` | `PD2` | `PSSI_D11` | 并行数据 bit11 |
| `CLK` | `PA8` | `TIM1_CH1` | STM32 输出�?AD9238 的采样时�?|
| `PWDN_EXT` | `PC0` | `AD9238_PWDN / GPIO_Output` | 输出低电平，AD9238 正常工作 |
| `OEB_EXT` | `PC1` | `AD9238_OEB / GPIO_Output` | 输出低电平，AD9238 数据输出使能 |
| `GND` | STM32 `GND` | 共地 | 必须共地 |
| `+5VIN` | 外部稳定 `5V` | 模块供电 | �?AD9238 模块供电 |

## 2. PSSI 采集时钟连接

PSSI 采集时钟直接使用 AD9238 �?`DCO`，接�?`PF8 / PSSI_PDCK`�?
必须做一根外部跳线：

| 输出�?| 输入�?| 说明 |
| --- | --- | --- |
| `AD9238 DCO` | `PF8 / PSSI_PDCK` | 使用 ADC 自带数据输出时钟驱动 PSSI |

也就是说�?
```text
AD9238 DCO  ----外部导线---->  PF8
```

如果不连接这根线，PSSI 没有 `PDCK` 时钟，DMA 不会正常采集 AD9238 数据�?
## 3. 可选信�?
| AD9238 模块信号 | 连接�?STM32H7R7 引脚 | STM32 功能 | 说明 |
| --- | --- | --- | --- |
| `OTR` | `PB7` | `AD9238_OTR / GPIO_Input` | 超量程指示，可选，建议连接 |

如果暂时不用 `OTR`，可以不接，但软件中读取 `AD9238_ReadOTR()` 的值就没有实际意义�?
## 4. 不需要连接的 PSSI 保留信号

工程�?PSSI 使用 16 bit 模式，但 AD9238 只有 12 bit 数据，因此下�?4 根线不接 AD9238�?
| STM32H7R7 引脚 | STM32 功能 | 连接方式 |
| --- | --- | --- |
| `PF11` | `PSSI_D12` | 不接 |
| `PG15` | `PSSI_D13` | 不接 |
| `PF2` | `PSSI_D14` | 不接 |
| `PF5` | `PSSI_D15` | 不接 |

软件中只取低 12 位：

```c
sample = raw_buf[i] & 0x0FFFu;
```

## 5. 不使用的 PSSI 控制信号

工程保留�?`PSSI_DE` �?`PSSI_RDY`，但 PSSI 参数中已经关�?DE/RDY 控制�?
```text
Control Signal = DE/RDY Disable
```

因此这两个引脚不要接 AD9238�?
| STM32H7R7 引脚 | STM32 功能 | 连接方式 |
| --- | --- | --- |
| `PG3` | `PSSI_DE` | 不接 |
| `PB4` | `PSSI_RDY` | 不接 |

## 6. 当前时钟频率

当前工程时钟树中�?
```text
TIM1 clock = 64 MHz
```

定时器参数：

| 信号 | 定时�?| ARR | Pulse | 输出频率 |
| --- | --- | ---: | ---: | ---: |
| `AD9238_CLK` | `TIM1_CH1 / PA8` | `39` | `20` | `1.6 MHz` |
| `PSSI_PDCK` | `AD9238 DCO -> PF8` | - | - | 跟随 ADC 数据输出时钟 |

关系�?
```text
CAP_CLK = 2 x AD9238_CLK
```

这适合先低速验�?PSSI + DMA 链路。要�?1 MHz 正弦并做正式计算，后续建议提高到�?
```text
AD9238_CLK = 5 MHz,  CAP_CLK = 10 MHz
�?AD9238_CLK = 10 MHz, CAP_CLK = 20 MHz
```

## 7. 当前代码采集流程

代码位置�?
```text
Appli\Core\Inc\ad9238_capture.h
Appli\Core\Src\ad9238_capture.c
```

启动顺序�?
```text
1. PWDN_EXT = 0
2. OEB_EXT  = 0
3. 启动 PSSI DMA 接收
4. 等待 AD9238 DCO 驱动 PF8
5. DMA 采满 raw_buf[]
6. 软件拆分 A/B 通道
```

默认采集缓存�?
```text
raw_buf[] = 4096 �?uint16_t
ch_a[]    = 2048 �?uint16_t
ch_b[]    = 2048 �?uint16_t
```

## 8. 串口输出到上位机

当前应用代码�?`Appli/Core/Src/main.c` 中手工初始化�?`USART2`，用于把每帧 AD9238 数据发送给上位机。注意：这部分是代码手工配置，不�?`.ioc` 中的 CubeMX 串口外设配置�?
默认串口参数�?
| 项目 | 当前�?|
| --- | --- |
| 串口 | `USART2` |
| TX | `PA3 / USART2_TX（SWAP 后）` |
| RX | `PA2 / USART2_RX（SWAP 后）` |
| 波特�?| `115200` |
| 数据格式 | `8N1` |

外部 USB 转串口连接：

```text
PA3 / USART2_TX -> CH340 RX
PA2 / USART2_RX <- CH340 TX
GND              -> USB 转串�?GND
```

## ADC 20 MHz 时钟

当前固件使用 `PC9 / MCO2` 输出 20 MHz，连接到 AD9238 的采样时钟输入�?
```text
PC9 / MCO2 (20 MHz) -> AD9238 CLK
GND                 -> AD9238 GND
```

原来�?`PA8 / TIM1_CH1` 不再启动，也不再作为 ADC 采样时钟�?`AD9238 DCO -> PF8 / PSSI_PDCK` 用于 STM32 �?PSSI 采集时钟；PA15/TIM2 已停用�?
发送格式为文本 CSV，每�?DMA 采满一帧后发送：

```text
AD9238_FRAME_BEGIN
raw_samples,4096
channel_samples,2048
format,index,ch_a,ch_b
0,通道A采样�?通道B采样�?1,通道A采样�?通道B采样�?...
AD9238_FRAME_END
```

`ch_a` �?`ch_b` 已经由软件从 AD9238 交替输出的原始数据中拆分。如果实际示波器/信号源验证发�?A/B 反了，把 `main.c` 中：

```c
AD9238_ProcessCapture(true);
```

改为�?
```c
AD9238_ProcessCapture(false);
```
